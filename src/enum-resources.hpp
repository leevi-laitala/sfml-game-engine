#pragma once

namespace System
{
    namespace Resources
    {
        enum Textures
        {
            Balloon,
            Heart,
            Dagger,
            Pop,
            Button
        };

        enum Fonts
        {
            Default
        };
    }

    enum Scenes
    {
        Mainmenu,
        Game
    };
}
