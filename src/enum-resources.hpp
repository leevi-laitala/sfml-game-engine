#pragma once

namespace System
{
    namespace Resources
    {
        enum class Textures
        {
            Default = 0,
            Balloon,
            Heart,
            Dagger,
            Pop,
            Button
        };

        enum class Fonts
        {
            Default = 0
        };

        enum class Sounds
        {
            Default = 0,
            ButtonPress
        };
    }

    enum class Scenes
    {
        Mainmenu,
        Game
    };
}
