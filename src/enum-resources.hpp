#pragma once

namespace System
{
    namespace Resources
    {
        enum Textures
        {
            Default = 0,
            Balloon,
            Heart,
            Dagger,
            Pop,
            Button
        };

        enum Fonts
        {
            Default = 0
        };

        enum Sounds
        {
            Default = 0
        }
    }

    enum Scenes
    {
        Mainmenu,
        Game
    };
}
